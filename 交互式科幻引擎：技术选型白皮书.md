# 技术选型与架构白皮书 (传统前后端分离版)

## 1. 架构愿景与选型原则

本项目旨在构建一个基于 AI 的科幻互动引擎。为了深入学习前后端分离架构及底层通信原理，采用传统的 SPA + 独立 Node.js 服务端模式。

## 2. 核心技术栈概览

| 模块         | 技术选型                         | 选用理由                                                     |
| ------------ | -------------------------------- | ------------------------------------------------------------ |
| **前端工程** | **React 18 + Vite**              | 极速的本地构建工具，替代 Create React App (CRA)。纯前端单页应用。 |
| **前端 UI**  | **Tailwind CSS + Framer Motion** | 快速构建响应式界面与平滑的科幻/故障风格动效。                |
| **独立后端** | **Node.js + Express.js**         | 经典的后端 Node 框架。负责路由分发、跨域处理 (CORS) 以及大模型接口转发。 |
| **AI 编排**  | **Vercel AI SDK**                | 核心组件。在 Express 环境下使用其提供的 `pipeDataStreamToResponse` 方案处理流式并发。 |
| **全局状态** | **Zustand**                      | 优雅管理 React 端的生命值、理智值与聊天历史流。              |
| **数据库**   | **Supabase (待接入)**            | PostgreSQL 驱动，后续为 Node 服务提供数据持久化与 `pgvector` 向量记忆支持。 |

## 3. 核心数据流转架构 (分离部署模式)

```
[浏览器: React App (端口: 5173)] 
      |
      | (POST /api/chat) -> 【需配置 CORS 跨域】
      | (SSE 文本流接收)
      v
[服务器: Node/Express (端口: 3001)] 
      |
      +--> (拦截/鉴权/组装 Prompt)
      |
      v
[LLM: OpenAI gpt-4o-mini API]
```

## 4. 工程目录规划建议

为了代码解耦，建议在项目根目录下建立两个完全独立的文件夹，分别维护各自的 `package.json`：

- `/frontend` (通过 `npm create vite@latest` 生成)
- `/backend` (手动初始化的 Express 项目)